[package]
name = "penny-config"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "0.8"
url = "2"

[dev-dependencies]
tempfile = "3"
